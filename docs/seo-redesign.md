# SEO and Apple-style redesign

The branch includes the responsive redesign, service dropdown, detailed service catalog, remote-only software installation, diagnosis source tags, mobile improvements and the new favicon.

Before merging, run `supabase/migrations/20260713073500_add_lead_source_tracking.sql` in Supabase SQL Editor. It adds the source tracking columns and `submit_service_request_v2`.
